name: easy_tasks
description: Easy tasks case study
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+17

environment:
  sdk: ">=2.10.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.0
  firebase_core: ^0.5.0+1
  firebase_auth: ^0.18.1+2
  google_sign_in: ^4.5.4
  cloud_firestore: ^0.14.1+3
  lint: ^1.3.0
  auto_route: ^0.6.7
  firebase_crashlytics: ^0.2.1+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  auto_route_generator: ^0.6.8
  build_runner:

flutter:
  uses-material-design: true
  assets:
    - images/
